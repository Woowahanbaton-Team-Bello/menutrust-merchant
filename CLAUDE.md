# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

### 서비스명

**MenuTrust**

### 서비스 정의

MenuTrust는 사장님이 메뉴판 사진을 업로드하면 메뉴명, 가격, 재료 후보, 알레르겐 후보를 확인·수정한 뒤 소비자용 알레르기 메뉴판과 QR을 발행할 수 있는 매장용 웹앱입니다.

이 저장소는 사장님(merchant)용 프론트엔드입니다. 손님이 QR로 접속하는 소비자용 메뉴판은 별도 화면 또는 별도 저장소 범위로 다룹니다.

## 사용자 흐름

1. 사장님이 로그인 또는 회원가입한다.
2. 메뉴판 이미지를 업로드한다.
3. AI 재료 분석 결과를 확인하고 재료를 체크·해제·추가·삭제한다.
4. AI 알레르겐 분석 결과를 확인하고 확정 / 혼입 가능 상태를 수정한다.
5. 메뉴판을 발행한다.
6. 공개 메뉴판 URL과 QR 코드를 확인한다.
7. 발행 후 홈 화면에서 메뉴판 내용을 확인하고 재발행하거나 수정한다.

## 핵심 도메인 개념

### 알레르기 분류 상태

선택한 알레르겐 기준으로 각 메뉴는 다음 셋 중 하나로 표시됩니다.

- **확정**: 해당 알레르겐이 메뉴에 확실히 포함됨. 원재료로 사용.
- **혼입 가능**: 원재료는 아니지만 조리 과정 등에서 섞여 들어갈 수 있음.
- **없음**: 해당 알레르겐이 포함되지 않음.

각 상태에는 **이유(reason)**가 함께 표시되어야 합니다. 손님이 상태만 보는 게 아니라 "왜 이 상태인지"를 확인하고 스스로 주문 여부를 판단하는 것이 이 서비스의 핵심 가치입니다.

### 알레르겐 목록

손님이 선택할 수 있는 알레르겐은 다음 22가지입니다. 한국 식약처 표시 대상 기준입니다.

알류(가금류), 우유, 메밀, 땅콩, 대두, 밀, 고등어, 게, 새우, 돼지고기, 복숭아, 토마토, 아황산류, 호두, 닭고기, 쇠고기, 오징어, 조개류, 굴, 전복, 홍합, 잣

이 목록은 도메인 상수입니다. 코드에서 하드코딩을 흩뿌리지 말고 한 곳에서 정의해 재사용하세요.

## 데모 매장

현재 데모 매장은 타이(태국) 음식점입니다. 샘플 메뉴 데이터는 타이 음식점을 기준으로 구성합니다.

## UI / UX 원칙

- 웹앱 우선: 사장님은 데스크톱·태블릿 환경에서 메뉴판을 검수할 가능성이 높으므로 좌측 내비게이션과 넓은 편집 영역을 기본으로 설계합니다.
- 조작 친화적: 재료·알레르겐 편집, 단계 이동, 발행 액션은 반복 작업이므로 명확한 버튼 크기와 간격을 유지합니다.
- 상태 가독성: 확정 / 혼입 가능 / 없음 3단계는 색·아이콘 등으로 즉시 구분되도록 하되, 색에만 의존하지 않고 텍스트/아이콘도 함께 제공합니다.
- 빠른 검수: 사장님이 누락·오탐 항목을 빠르게 고칠 수 있도록 메뉴 단위 카드와 상태 토글을 간결하게 보여줍니다.

## 기술 스택

- React 19 + Vite (`@vitejs/plugin-react`)
- 빌드 도구: Vite
- 린터: oxlint (`.oxlintrc.json`)
- 언어: JavaScript (JSX), ESM (`"type": "module"`)

## 명령어

```bash
npm run dev      # 개발 서버 (Vite, HMR)
npm run build    # 프로덕션 빌드
npm run preview  # 빌드 결과 미리보기
npm run lint     # oxlint 실행
```

## 프로젝트 구조

```text
index.html
src/
  main.jsx
  App.jsx
  App.css
  index.css
  domain/
    allergy.js
    merchantDemo.js
  assets/
public/
```

## 작업 시 주의사항

- 알레르겐 22종 목록과 3단계 상태값은 도메인 상수이므로 단일 소스에서 관리합니다.
- 새 기능 작업은 `develop`에서 `feature/*` 브랜치를 생성해 진행합니다.
- 새 화면·컴포넌트는 사장님용 웹앱 레이아웃을 먼저 고려해 작성합니다.
- 상태(확정/혼입 가능/없음)를 표시할 때는 항상 그 이유를 함께 보여줄 수 있는 구조로 만듭니다.
